## Agent skills

### Issue tracker

Issues for this repository are tracked in the Linear `API` project under the `Acolhe` team. See `docs/agents/issue-tracker.md`.

### Triage labels

The tracker uses the five default triage labels. See `docs/agents/triage-labels.md`.

### Domain docs

This repository uses the single-context layout. See `docs/agents/domain.md`.
