# Stack de deploy (roda na VM Oracle). A imagem já vem pronta do GHCR —
# a VM NÃO compila nada, só puxa e roda. Uma cópia deste arquivo roda em
# cada VM (staging e prod); os segredos ficam no .env ao lado (chmod 600).
name: acolhe

services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - pgdata:/var/lib/postgresql/data
    # tuning p/ VM de 1GB de RAM
    command:
      - "postgres"
      - "-c"
      - "shared_buffers=128MB"
      - "-c"
      - "max_connections=50"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 10
    # sem `ports:` — Postgres fica só na rede interna do compose, nunca exposto

  backup:
    image: postgres:16-alpine
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      PGHOST: postgres
      PGPORT: "5432"
      PGDATABASE: ${POSTGRES_DB}
      PGUSER: ${POSTGRES_USER}
      PGPASSWORD: ${POSTGRES_PASSWORD}
      BACKUP_CRON: "${BACKUP_CRON:-0 3 * * *}"
      BACKUP_RETENTION_DAYS: "${BACKUP_RETENTION_DAYS:-7}"
    entrypoint: ["sh", "/scripts/entrypoint.sh"]
    volumes:
      - ./backup:/scripts:ro
      - ./backups:/backups

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: ["redis-server", "--save", "", "--appendonly", "yes", "--appendfsync", "everysec", "--maxmemory", "64mb", "--maxmemory-policy", "noeviction"]
    volumes:
      - redisdata:/data
    # sem `ports:` — Redis fica só na rede interna

  # Garante que API e worker usem um role sem BYPASSRLS. É idempotente e também
  # atualiza grants para bancos já existentes antes de aplicar novas migrations.
  db-role:
    image: postgres:16-alpine
    restart: "no"
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      PGPASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_APP_PASSWORD: ${POSTGRES_APP_PASSWORD}
    entrypoint: ["sh", "/scripts/configure-app-role.sh"]
    volumes:
      - ./postgres-init:/scripts:ro

  # Aplica migrations pendentes (Atlas) antes da API/worker subirem. Roda e sai.
  migrate:
    image: ${IMAGE}
    pull_policy: always
    restart: "no"
    depends_on:
      postgres:
        condition: service_healthy
      db-role:
        condition: service_completed_successfully
    entrypoint: ["atlas", "migrate", "apply", "--dir", "file:///migrations", "--url", "${DATABASE_URL}"]

  api:
    image: ${IMAGE}
    pull_policy: always
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    environment:
      PORT: "8080"
      DATABASE_URL: ${APP_DATABASE_URL}
      REDIS_ADDR: redis:6379
      JWT_SECRET: ${JWT_SECRET}
    # sem `ports:` — quem expõe pra internet é o Caddy (HTTPS). A API fica interna.
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  worker:
    image: ${IMAGE}
    pull_policy: always
    restart: unless-stopped
    depends_on:
      redis:
        condition: service_started
      migrate:
        condition: service_completed_successfully
    entrypoint: ["worker"]
    environment:
      DATABASE_URL: ${APP_DATABASE_URL}
      REDIS_ADDR: redis:6379

  # Reverse proxy com HTTPS automático (Let's Encrypt/ZeroSSL) para ${CADDY_DOMAIN}.
  # CADDY_DOMAIN é injetado pelo workflow de deploy a partir do IP (ex.: 1.2.3.4.nip.io).
  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    depends_on:
      api:
        condition: service_healthy
    command: caddy reverse-proxy --from ${CADDY_DOMAIN:-localhost} --to api:8080
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - caddy_data:/data # certificados — persistir evita re-emitir (rate limit)
      - caddy_config:/config

volumes:
  pgdata:
  redisdata:
  caddy_data:
  caddy_config:
